# dovi-bulk-encoder
dovi-bulk-encoder automates bulk Dolby Vision Profile 7 encoding using Dolby Encoding Engine (DEE). It watches a folder for supported input video files and DolbyMetadata.xml, then runs DEE with settings from a JSON config. Requires an active DEE license and Python 3.13.
